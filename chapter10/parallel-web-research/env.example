# 复制为 .env 后按需填写。全部留空也能跑通（走离线关键词判断，结果可复现）。

# —— 可选：让子 Agent 用真实 LLM 判断"网页是否命中答案" ——
# 支持 OpenAI 兼容接口。可用的 key：OPENAI_API_KEY / MOONSHOT_API_KEY / ARK_API_KEY
# （请勿使用 OPENROUTER / ANTHROPIC / DEEPSEEK / SILICONFLOW，均已失效。）
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# 自定义 OpenAI 兼容网关时填写，例如 Moonshot / 火山方舟 ARK：
# OPENAI_BASE_URL=https://api.moonshot.cn/v1
# OPENAI_MODEL=kimi-k2-0905-preview

# 强制离线（即使配置了 key 也走关键词判断），设为 1/true 生效：
USE_LLM=
